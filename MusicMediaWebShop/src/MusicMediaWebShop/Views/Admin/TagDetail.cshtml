@model IEnumerable<TagDetail>
@{
    ViewData["Title"] = "Admin_TagDetail";
    Layout = "_AdminLayout";
}
<div id="page-wrapper">
    <div class="row" style="margin-bottom:15px">

        <a class="btn btn-success" asp-action="TagDetailAdd" asp-controller="Admin">
            <label>Add New Tag Detail</label>
            <i class="fa fa-plus-circle"></i>
        </a>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Tag Detail
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Tag Detail Name</th>
                                <th>Belong Tag</th>
                                <th>Number of Product</th>                                
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var tag in Model)
                            {
                                <tr class="odd gradeX">
                                    <td>@tag.TagDetailName</td>
                                    <td><a asp-action="TagDetailTag" asp-controller="Admin" asp-route-id="@tag.TagDetailID">@tag.TagSupports.Count</a></td>
                                    <td><a asp-action="TagDetailProduct" asp-controller="Admin" asp-route-id="@tag.TagDetailID">@tag.TagHelper.Count</a></td>                                   
                                    <td>
                                        <a asp-action="TagDetailEdit" asp-controller="Admin" asp-route-id="@tag.TagDetailID" class="btn btn-sm btn-info">
                                            <i class="fa fa-pencil"></i>
                                        </a>
                                        <a asp-action="TagDetailDelete" asp-controller="Admin" asp-route-id="@tag.TagDetailID" class="btn btn-sm btn-danger">
                                            <i class="fa fa-eraser"></i>
                                        </a>
                                    </td>
                                </tr>
                            }


                        </tbody>
                    </table>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
</div>



